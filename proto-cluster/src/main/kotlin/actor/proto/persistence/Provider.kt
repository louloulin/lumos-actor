package actor.proto.persistence

/**
 * Provider is a mock interface for persistence providers.
 * This is a temporary solution until the real persistence module is implemented.
 */
interface Provider
